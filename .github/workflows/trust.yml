name: trust

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  trust:
    runs-on: ubuntu-latest
    container: swift:6.0
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Fetch trusted base history
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git fetch origin main:refs/remotes/origin/main
      - name: Install Trust runtime dependencies
        run: apt-get update && apt-get install -y python3 curl
      - name: Resolve comparison range
        id: comparison
        run: echo "range=${{ github.event.pull_request.base.sha || github.event.before }}..${{ github.event.pull_request.head.sha || github.sha }}" >> "$GITHUB_OUTPUT"
      - name: CorvidLabs Trust gate
        id: trust
        uses: CorvidLabs/trust@e0272543ad5c88ec75cea1e606cc2efa552af25b # v1.2.0-rc.4
        with:
          range: ${{ steps.comparison.outputs.range }}
      - name: Enforce the verified SpecSync lifecycle
        uses: CorvidLabs/spec-sync@29392630a590c13fcca65bec2a3c0a8f8c9e4081 # v6.0.0-rc.12
        with:
          version: "5.1.1"
          strict: true
          require-coverage: 100
          lifecycle-enforce: true
